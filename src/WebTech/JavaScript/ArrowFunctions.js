var func =(a,b) => a+b;
console.log(func(10,20))



// Basic syntax
let myFunction = (param1, param2) => expression;

// With a single parameter (parentheses can be omitted)
let singleParamFunction = param => expression;

// With no parameters
let noParamFunction = () => expression;

// With a block body (requires explicit return)
let blockBodyFunction = (param1, param2) => {
  // multiple statements
  return value;
};